openapi: 3.0.3
info:
  title: Ecoplay API
  description: |
    API REST da Ecoplay. Base JSON: `/api`.

    **Autenticação:** `POST /api/auth/login` devolve `token`. Nas restantes rotas (exceto login e registo) envia o header `Authorization: Bearer <token>`.

    **Registo:** `POST /api/usuarios` (público).
  version: 1.0.0

servers:
  - url: http://localhost:3000
    description: Local — em produção, usa "Try it out" com o URL público do teu serviço (ex. Render)

tags:
  - name: Raiz e saúde
    description: Endpoints fora do prefixo /api ou utilitários
  - name: Auth
    description: Login e sessão JWT
  - name: Usuários
  - name: Missões
  - name: Utilizador — missões
  - name: Doações
  - name: Itens
  - name: Utilizador — itens
  - name: Avatares
  - name: Utilizador — avatares
  - name: Amizades
  - name: Instituições
  - name: Títulos
  - name: Utilizador — títulos
  - name: Conquistas
  - name: Utilizador — conquistas

paths:
  /:
    get:
      tags: [Raiz e saúde]
      summary: Informação da API
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok: { type: boolean }
                  message: { type: string }

  /health:
    get:
      tags: [Raiz e saúde]
      summary: Estado do serviço (Postgres / Supabase)
      responses:
        "200":
          description: Serviço considerado saudável
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
        "503":
          description: Algum check falhou
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"

  /api/supabase-ready:
    get:
      tags: [Raiz e saúde]
      summary: Verifica se o cliente Supabase pode ser inicializado
      responses:
        "200":
          description: OK
        "503":
          description: Variáveis em falta
        "500":
          description: Erro

  /api/auth/login:
    post:
      tags: [Auth]
      summary: Login (obtém JWT)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
            example:
              email: user@exemplo.com
              senha: "secret"
      responses:
        "200":
          description: Token + dados do utilizador (sem senha)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "400":
          description: Pedido inválido
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorMessage"
        "401":
          description: Credenciais inválidas
        "503":
          description: JWT não configurado no servidor

  /api/auth/me:
    get:
      tags: [Auth]
      summary: Perfil do utilizador autenticado
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        "401":
          description: Token em falta ou inválido
        "404":
          description: Utilizador não encontrado

  /api/usuarios:
    get:
      tags: [Usuários]
      summary: Listar utilizadores
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Usuario"
    post:
      tags: [Usuários]
      summary: Registar utilizador (público)
      description: A senha é armazenada com hash (bcrypt).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        "400":
          description: Validação
        "409":
          description: Email já registado

  /api/usuarios/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: integer
    get:
      tags: [Usuários]
      summary: Obter utilizador com relações
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        "404":
          description: Não encontrado
    put:
      tags: [Usuários]
      summary: Atualizar utilizador (substituição)
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
        "404":
          description: Não encontrado
    patch:
      tags: [Usuários]
      summary: Atualizar utilizador (parcial)
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Usuario"
    delete:
      tags: [Usuários]
      summary: Eliminar utilizador e dados associados
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo
        "404":
          description: Não encontrado

  /api/missoes:
    get:
      tags: [Missões]
      security:
        - bearerAuth: []
      summary: Listar missões
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Missao"
    post:
      tags: [Missões]
      security:
        - bearerAuth: []
      summary: Criar missão
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MissaoInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Missao"

  /api/missoes/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Missões]
      security:
        - bearerAuth: []
      summary: Obter missão
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Missao"
        "404":
          description: Não encontrado
    put:
      tags: [Missões]
      security:
        - bearerAuth: []
      summary: Atualizar missão
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MissaoInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Missao"
    patch:
      tags: [Missões]
      security:
        - bearerAuth: []
      summary: Atualizar missão (parcial)
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MissaoInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Missao"
    delete:
      tags: [Missões]
      security:
        - bearerAuth: []
      summary: Eliminar missão
      responses:
        "204":
          description: Sem conteúdo
        "404":
          description: Não encontrado

  /api/usuario-missoes:
    get:
      tags: [Utilizador — missões]
      security:
        - bearerAuth: []
      summary: Listar associações utilizador–missão
      parameters:
        - name: usuario_id
          in: query
          schema:
            type: integer
        - name: missao_id
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UsuarioMissao"
    post:
      tags: [Utilizador — missões]
      security:
        - bearerAuth: []
      summary: Associar utilizador a uma missão
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioMissaoCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioMissao"

  /api/usuario-missoes/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Utilizador — missões]
      security:
        - bearerAuth: []
      summary: Obter registo
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioMissao"
    put:
      tags: [Utilizador — missões]
      security:
        - bearerAuth: []
      summary: Atualizar (ex. status, data_conclusao)
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioMissaoUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioMissao"
    patch:
      tags: [Utilizador — missões]
      security:
        - bearerAuth: []
      summary: Atualizar parcial
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioMissaoUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioMissao"
    delete:
      tags: [Utilizador — missões]
      security:
        - bearerAuth: []
      summary: Remover associação
      responses:
        "204":
          description: Sem conteúdo

  /api/doacoes:
    get:
      tags: [Doações]
      security:
        - bearerAuth: []
      summary: Listar doações
      parameters:
        - name: usuario_id
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Doacao"
    post:
      tags: [Doações]
      security:
        - bearerAuth: []
      summary: Criar doação
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DoacaoCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Doacao"

  /api/doacoes/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Doações]
      security:
        - bearerAuth: []
      summary: Obter doação
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Doacao"
    put:
      tags: [Doações]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DoacaoCreate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Doacao"
    patch:
      tags: [Doações]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DoacaoCreate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Doacao"
    delete:
      tags: [Doações]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo

  /api/itens:
    get:
      tags: [Itens]
      security:
        - bearerAuth: []
      summary: Listar itens da loja
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Item"
    post:
      tags: [Itens]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ItemInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"

  /api/itens/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Itens]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"
    put:
      tags: [Itens]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ItemInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"
    patch:
      tags: [Itens]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ItemInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Item"
    delete:
      tags: [Itens]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo

  /api/usuario-itens:
    get:
      tags: [Utilizador — itens]
      security:
        - bearerAuth: []
      parameters:
        - name: usuario_id
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UsuarioItem"
    post:
      tags: [Utilizador — itens]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioItemCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioItem"

  /api/usuario-itens/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Utilizador — itens]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioItem"
    delete:
      tags: [Utilizador — itens]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo

  /api/avatares:
    get:
      tags: [Avatares]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Avatar"
    post:
      tags: [Avatares]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AvatarInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Avatar"

  /api/avatares/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Avatares]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Avatar"
    put:
      tags: [Avatares]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AvatarInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Avatar"
    patch:
      tags: [Avatares]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AvatarInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Avatar"
    delete:
      tags: [Avatares]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo

  /api/usuario-avatares:
    get:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      parameters:
        - name: usuario_id
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UsuarioAvatar"
    post:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioAvatarCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioAvatar"

  /api/usuario-avatares/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioAvatar"
    put:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioAvatarUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioAvatar"
    patch:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioAvatarUpdate"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioAvatar"
    delete:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo

  /api/usuario-avatares/{id}/ativar:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    patch:
      tags: [Utilizador — avatares]
      security:
        - bearerAuth: []
      summary: Definir este avatar como ativo (desativa os outros do utilizador)
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioAvatar"

  /api/amizades:
    get:
      tags: [Amizades]
      security:
        - bearerAuth: []
      summary: Listar amizades de um utilizador
      parameters:
        - name: usuario_id
          in: query
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Amizade"
        "400":
          description: usuario_id em falta
    post:
      tags: [Amizades]
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AmizadeCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Amizade"

  /api/amizades/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Amizades]
      security:
        - bearerAuth: []
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Amizade"
    delete:
      tags: [Amizades]
      security:
        - bearerAuth: []
      responses:
        "204":
          description: Sem conteúdo

  /api/instituicoes:
    get:
      tags: [Instituições]
      security:
        - bearerAuth: []
      summary: Listar instituições
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Instituicao"
    post:
      tags: [Instituições]
      security:
        - bearerAuth: []
      summary: Criar instituição
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InstituicaoInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Instituicao"

  /api/instituicoes/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Instituições]
      security:
        - bearerAuth: []
      summary: Obter instituição
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Instituicao"
        "404":
          description: Não encontrado
    put:
      tags: [Instituições]
      security:
        - bearerAuth: []
      summary: Atualizar instituição
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InstituicaoInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Instituicao"
    patch:
      tags: [Instituições]
      security:
        - bearerAuth: []
      summary: Atualizar instituição (parcial)
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InstituicaoInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Instituicao"
    delete:
      tags: [Instituições]
      security:
        - bearerAuth: []
      summary: Eliminar instituição
      responses:
        "204":
          description: Sem conteúdo
        "404":
          description: Não encontrado

  /api/titulos:
    get:
      tags: [Títulos]
      security:
        - bearerAuth: []
      summary: Listar títulos
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Titulo"
    post:
      tags: [Títulos]
      security:
        - bearerAuth: []
      summary: Criar título
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TituloInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Titulo"

  /api/titulos/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Títulos]
      security:
        - bearerAuth: []
      summary: Obter título
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Titulo"
        "404":
          description: Não encontrado
    put:
      tags: [Títulos]
      security:
        - bearerAuth: []
      summary: Atualizar título
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TituloInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Titulo"
    patch:
      tags: [Títulos]
      security:
        - bearerAuth: []
      summary: Atualizar título (parcial)
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TituloInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Titulo"
    delete:
      tags: [Títulos]
      security:
        - bearerAuth: []
      summary: Eliminar título
      responses:
        "204":
          description: Sem conteúdo
        "404":
          description: Não encontrado

  /api/usuario-titulos:
    get:
      tags: [Utilizador — títulos]
      security:
        - bearerAuth: []
      summary: Listar associações utilizador–título
      parameters:
        - name: usuario_id
          in: query
          schema:
            type: integer
        - name: titulo_id
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UsuarioTitulo"
    post:
      tags: [Utilizador — títulos]
      security:
        - bearerAuth: []
      summary: Associar utilizador a um título
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioTituloCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioTitulo"

  /api/usuario-titulos/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Utilizador — títulos]
      security:
        - bearerAuth: []
      summary: Obter registo
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioTitulo"
    delete:
      tags: [Utilizador — títulos]
      security:
        - bearerAuth: []
      summary: Remover associação
      responses:
        "204":
          description: Sem conteúdo

  /api/conquistas:
    get:
      tags: [Conquistas]
      security:
        - bearerAuth: []
      summary: Listar conquistas
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Conquista"
    post:
      tags: [Conquistas]
      security:
        - bearerAuth: []
      summary: Criar conquista
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConquistaInput"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conquista"

  /api/conquistas/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Conquistas]
      security:
        - bearerAuth: []
      summary: Obter conquista
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conquista"
        "404":
          description: Não encontrado
    put:
      tags: [Conquistas]
      security:
        - bearerAuth: []
      summary: Atualizar conquista
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConquistaInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conquista"
    patch:
      tags: [Conquistas]
      security:
        - bearerAuth: []
      summary: Atualizar conquista (parcial)
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ConquistaInput"
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Conquista"
    delete:
      tags: [Conquistas]
      security:
        - bearerAuth: []
      summary: Eliminar conquista
      responses:
        "204":
          description: Sem conteúdo
        "404":
          description: Não encontrado

  /api/usuario-conquistas:
    get:
      tags: [Utilizador — conquistas]
      security:
        - bearerAuth: []
      summary: Listar associações utilizador–conquista
      parameters:
        - name: usuario_id
          in: query
          schema:
            type: integer
        - name: conquista_id
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UsuarioConquista"
    post:
      tags: [Utilizador — conquistas]
      security:
        - bearerAuth: []
      summary: Associar utilizador a uma conquista
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UsuarioConquistaCreate"
      responses:
        "201":
          description: Criado
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioConquista"

  /api/usuario-conquistas/{id}:
    parameters:
      - $ref: "#/components/parameters/IdPath"
    get:
      tags: [Utilizador — conquistas]
      security:
        - bearerAuth: []
      summary: Obter registo
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UsuarioConquista"
    delete:
      tags: [Utilizador — conquistas]
      security:
        - bearerAuth: []
      summary: Remover associação
      responses:
        "204":
          description: Sem conteúdo

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Obtido em POST /api/auth/login (campo `token`).

  parameters:
    IdPath:
      name: id
      in: path
      required: true
      schema:
        type: integer

  schemas:
    ErrorMessage:
      type: object
      properties:
        error:
          type: string

    HealthResponse:
      type: object
      properties:
        ok:
          type: boolean
        checks:
          type: object
          additionalProperties: true

    LoginRequest:
      type: object
      required: [email, senha]
      properties:
        email:
          type: string
          format: email
        senha:
          type: string
          format: password

    LoginResponse:
      type: object
      properties:
        token:
          type: string
        usuario:
          $ref: "#/components/schemas/Usuario"

    Usuario:
      type: object
      properties:
        id: { type: integer }
        nome: { type: string, nullable: true }
        email: { type: string }
        nivel: { type: integer }
        xp: { type: integer }
        pontos_total: { type: integer }
        pontos_semana: { type: integer }
        titulo_atual: { type: string, nullable: true }
        criado_em: { type: string, format: date-time, nullable: true }
        atualizado_em: { type: string, format: date-time, nullable: true }

    UsuarioCreate:
      type: object
      required: [email, senha]
      properties:
        nome: { type: string }
        email: { type: string, format: email }
        senha: { type: string, format: password }
        nivel: { type: integer }
        xp: { type: integer }
        pontos_total: { type: integer }
        pontos_semana: { type: integer }
        titulo_atual: { type: string }

    UsuarioUpdate:
      type: object
      properties:
        nome: { type: string }
        email: { type: string }
        senha: { type: string, format: password, description: "Nova senha (será hasheada)" }
        nivel: { type: integer }
        xp: { type: integer }
        pontos_total: { type: integer }
        pontos_semana: { type: integer }
        titulo_atual: { type: string }

    Missao:
      type: object
      properties:
        id: { type: integer }
        titulo: { type: string, nullable: true }
        descricao: { type: string, nullable: true }
        nivel: { type: integer }
        xp_recompensa: { type: integer }
        pontos_recompensa: { type: integer }
        progresso_total: { type: integer }
        url_imagem: { type: string, nullable: true }
        criada_em: { type: string, format: date-time, nullable: true }

    MissaoInput:
      type: object
      properties:
        titulo: { type: string }
        descricao: { type: string }
        nivel: { type: integer }
        xp_recompensa: { type: integer }
        pontos_recompensa: { type: integer }
        progresso_total: { type: integer }
        url_imagem: { type: string }

    UsuarioMissao:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        missao_id: { type: integer }
        progresso_atual: { type: integer }
        status: { type: string, description: "ex. pending, completed" }
        concluida_em: { type: string, format: date-time, nullable: true }

    UsuarioMissaoCreate:
      type: object
      required: [usuario_id, missao_id]
      properties:
        usuario_id: { type: integer }
        missao_id: { type: integer }
        progresso_atual: { type: integer }
        status: { type: string }
        concluida_em: { type: string, format: date-time }

    UsuarioMissaoUpdate:
      type: object
      properties:
        progresso_atual: { type: integer }
        status: { type: string }
        concluida_em: { type: string, format: date-time }

    Doacao:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        instituicao_id: { type: integer }
        descricao: { type: string, nullable: true }
        quantidade: { type: integer }
        data: { type: string, format: date-time, nullable: true }

    DoacaoCreate:
      type: object
      required: [usuario_id, instituicao_id]
      properties:
        usuario_id: { type: integer }
        instituicao_id: { type: integer }
        descricao: { type: string }
        quantidade: { type: integer }
        data: { type: string, format: date-time }

    Item:
      type: object
      properties:
        id: { type: integer }
        nome: { type: string, nullable: true }
        descricao: { type: string, nullable: true }
        custo_pontos: { type: integer }
        url_imagem: { type: string, nullable: true }

    ItemInput:
      type: object
      properties:
        nome: { type: string }
        descricao: { type: string }
        custo_pontos: { type: integer }
        url_imagem: { type: string }

    UsuarioItem:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        item_id: { type: integer }
        adquirido_em: { type: string, format: date-time, nullable: true }

    UsuarioItemCreate:
      type: object
      required: [usuario_id, item_id]
      properties:
        usuario_id: { type: integer }
        item_id: { type: integer }
        adquirido_em: { type: string, format: date-time }

    Avatar:
      type: object
      properties:
        id: { type: integer }
        nome: { type: string, nullable: true }
        url_imagem: { type: string, nullable: true }

    AvatarInput:
      type: object
      properties:
        nome: { type: string }
        url_imagem: { type: string }

    UsuarioAvatar:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        avatar_id: { type: integer }
        ativo: { type: boolean }

    UsuarioAvatarCreate:
      type: object
      required: [usuario_id, avatar_id]
      properties:
        usuario_id: { type: integer }
        avatar_id: { type: integer }
        ativo: { type: boolean }

    UsuarioAvatarUpdate:
      type: object
      properties:
        ativo: { type: boolean, description: "Se true, ativa este registo e desativa os outros do mesmo utilizador" }

    Amizade:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        amigo_id: { type: integer }

    AmizadeCreate:
      type: object
      required: [usuario_id, amigo_id]
      properties:
        usuario_id: { type: integer }
        amigo_id: { type: integer }

    Instituicao:
      type: object
      properties:
        id: { type: integer }
        nome: { type: string }
        tipo: { type: string, nullable: true }
        endereco: { type: string, nullable: true }
        telefone: { type: string, nullable: true }

    InstituicaoInput:
      type: object
      required: [nome, tipo, endereco, telefone]
      properties:
        nome: { type: string }
        tipo: { type: string }
        endereco: { type: string }
        telefone: { type: string }

    Titulo:
      type: object
      properties:
        id: { type: integer }
        nome: { type: string }
        descricao: { type: string, nullable: true }
        xp_necessario: { type: integer }

    TituloInput:
      type: object
      required: [nome, xp_necessario]
      properties:
        nome: { type: string }
        descricao: { type: string }
        xp_necessario: { type: integer }

    UsuarioTitulo:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        titulo_id: { type: integer }
        desbloqueado_em: { type: string, format: date-time, nullable: true }

    UsuarioTituloCreate:
      type: object
      required: [usuario_id, titulo_id]
      properties:
        usuario_id: { type: integer }
        titulo_id: { type: integer }
        desbloqueado_em: { type: string, format: date-time }

    Conquista:
      type: object
      properties:
        id: { type: integer }
        nome: { type: string }
        descricao: { type: string, nullable: true }
        url_icone: { type: string, nullable: true }
        xp_recompensa: { type: integer }

    ConquistaInput:
      type: object
      required: [nome, xp_recompensa]
      properties:
        nome: { type: string }
        descricao: { type: string }
        url_icone: { type: string }
        xp_recompensa: { type: integer }

    UsuarioConquista:
      type: object
      properties:
        id: { type: integer }
        usuario_id: { type: integer }
        conquista_id: { type: integer }
        desbloqueada_em: { type: string, format: date-time, nullable: true }

    UsuarioConquistaCreate:
      type: object
      required: [usuario_id, conquista_id]
      properties:
        usuario_id: { type: integer }
        conquista_id: { type: integer }
        desbloqueada_em: { type: string, format: date-time }
